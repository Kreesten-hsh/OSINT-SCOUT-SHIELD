# --- BASE DE DONNÉES (PostgreSQL) ---
POSTGRES_USER=osint_user
POSTGRES_PASSWORD=osint_password
POSTGRES_DB=osint_db
POSTGRES_HOST=db
POSTGRES_PORT=5432

# --- BACKEND (FastAPI) ---
# Clé secrète pour le chiffrement (JWT) - À changer en prod !
SECRET_KEY=votre_super_cle_secrete_aleatoire_et_longue
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# URL de connexion DB pour SQLAlchemy
DATABASE_URL=postgresql+asyncpg://osint_user:osint_password@db:5432/osint_db

# --- REDIS (Cache & Workers) ---
REDIS_URL=redis://redis:6379/0

# --- FRONTEND (Vite) ---
VITE_API_URL=http://localhost:8000
